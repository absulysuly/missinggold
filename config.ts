// Application-wide configuration settings.

export const config = {
  // Number of events to display per page in paginated views.
  EVENTS_PER_PAGE: 8,
  
  // App name
  APP_NAME: 'Eventara',

  // Default language
  DEFAULT_LANG: 'en',
};
